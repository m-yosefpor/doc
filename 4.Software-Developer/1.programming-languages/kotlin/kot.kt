apt-get install libncurses5
snap install kotlin

wget ....
add kotlin-akfalfaj/bin to path
----------


def f(int a, int b):
    return a


#kotlin vs java

no ;
infered return type, so fun keyword for functions
infered assignment type



- why not "Int a" instead of a:Int ?
- why not "int" instead of "Int"? (class start with Capital and camel case?
- for (a in b) {    ::   for a in b {
- if (true)   : if true
- "hi" instead of 'hi'

- why val/var (at least optional like js
- can not overwrite a var with a new value type (autocast), have to use var a = 3.4

- Int a()   #   fun a():Int


------------
(standard)

def = fun
val , var
and or not : && || !
true false | True, False
commenting  : # not working , instead //




: instead of {} and spacing?


println , print


a..b step c
a downTo b step c
